apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: app-demo-afonso-location
  description: Location to import all app-demo-afonso catalog components
spec:
  targets:
    - ./.platform/*.yaml
